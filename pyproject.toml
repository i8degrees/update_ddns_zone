
[build-system]
requires = [
  "setuptools>=61.0"
]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">=3.11"
name = "update_ddns_zone"
version = "1.0.0"
description = "DDNS DHCP update integration with DNSMASQ"
readme = "README.md"
#license = "BSD"
license = { file = "LICENSE.md" }
authors = [{ name = "Jeffrey Carpenter", email = "1329364+i8degrees@users.noreply.github.com" }]
keywords = [ "ddns", "dhcp", "dnsmasq", "powerdns", "pdns" ]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3",
    # !! OpenWRT v24.10.4 requires this particular version
    "Programming Language :: Python :: 3.11",
    "Topic :: Utilities",
]

dependencies = [
  "python-dotenv >= 1.2.1",
  "python-dotenv[cli] >= 1.2.1",
  "urllib3 >= 2.6.3",
  # !! OpenWRT v24.10.4 requires this particular version
  "pyyaml == 6.0.1",
]

[project.optional-dependencies]
dev = [
  "validate-pyproject[all]",
  "mypy==1.19.1",
  # !! TODO Verify whether or not we still need this dep
  "types-requests==2.32.4.20260107",
  "typing_extensions==4.15.0",
]

[project.urls]
"Homepage" = "https://github.com/i8degrees/update_ddns_zone.git"
"Bug Reports" = "https://github.com/i8degrees/update_ddns_zone.git"
"Source" = "https://github.com/i8degrees/update_ddns_zone.git"
"Documentation" = "https://github.com/i8degrees/update_ddns_zone.git"

[project.scripts]
ddns_update = "app.__main__:main"
utils = "utils.__main__:main"
test = "test.__main__:main"
# !! Future impl updates
#ddns_psupdate = "psupdate.__main__:main"
#ddns_nsupdate = "nsupdate.__main__:main"
#nomutils = "utils.__main__:main"
#build = "scripts.build:main"
#lint = "lint:main"

#[tool.setuptools]
#py-modules = ["build", "analyze", "lint"]
#py-modules = ["build", "deploy", "test", "ddns_update"]

